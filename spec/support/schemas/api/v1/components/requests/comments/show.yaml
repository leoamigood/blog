type: object
required:
  - data
properties:
  data:
    $ref: "<%= draw_openapi_ref(:models, :Comment) %>"
  jsonapi:
    $ref: "<%= draw_openapi_ref(:shared, :jsonapi) %>"
  meta:
    $ref: "<%= draw_openapi_ref(:shared, :empty_meta) %>"
