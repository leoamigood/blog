type: object
required:
  - data
  - jsonapi
properties:
  data:
    $ref: "<%= draw_openapi_ref(:models, :Post) %>"
  jsonapi:
    $ref: "<%= draw_openapi_ref(:shared, :jsonapi) %>"
  meta:
    $ref: "<%= draw_openapi_ref(:shared, :empty_meta) %>"
